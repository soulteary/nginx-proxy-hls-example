# Nginx Proxy HLS Example

A simple example to demonstrate Nginx native capabilities: proxy hls.

一个演示 Nginx 原生功能的简单示例：转发 HLS 直播。

In the common live broadcast and rebroadcast of conferences, there are actually simpler solutions.

在常见的会议直播、转播过程中，其实有更简单的方案，就是使用 Nginx 原生代理能力。
